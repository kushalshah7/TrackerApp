const json=async(path:string,init?:RequestInit)=>{const r=await fetch(`/api${path}`,init);const body=await r.json().catch(()=>({}));if(!r.ok)throw new Error(body.detail||'Something went wrong');return body};
export const api={health:()=>json('/health'),entries:(m:string,limit=100)=>json(`/entries/${m}?limit=${limit}`),add:(m:string,data:any)=>json(`/entries/${m}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({data})}),update:(m:string,row:number,data:any)=>json(`/entries/${m}/${row}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({data})}),sheet:(p:string)=>json(p)};
